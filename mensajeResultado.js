// =============================
// 📌 Generador de mensajes de resultado
// =============================

// Esta función ya no devuelve ningún mensaje final.
// Sirve solo para mantener la compatibilidad con el resto del código.
async function generarMensajeResultado(dni, resultado, textoExtra = '', dominioData = null) {
  return {
    mensajePrincipal: '',   // no enviamos nada
    mensajeVacunas: ''      // no enviamos nada
  };
}

module.exports = generarMensajeResultado;





















